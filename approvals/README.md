# Approvals — pending Kevin's sign-off

Anything here needs a human decision before it happens — same pattern as
the Etsy team's `approvals/`. Each proposal is its own file:
`YYYY-MM-DD-short-description.md`.
